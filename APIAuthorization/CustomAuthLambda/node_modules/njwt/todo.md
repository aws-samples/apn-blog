what happens when i do this? var jwt = nJwt.create({},secret);

add nbf checking

jsdoc all the thigns


enforce codde formatting rules

benchmarks: how many JWTS per second can be created/verified, compared to other libraries?